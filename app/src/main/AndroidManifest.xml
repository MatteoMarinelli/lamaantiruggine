<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.marinelli.lamaantiruggine">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Politica"
            android:label="@string/title_activity_politica"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".Lifestyle"
            android:label="Lifestyle"
            android:theme="@style/FullscreenTheme"/>
        <activity android:name=".Cinema"
            android:label="Cinema"
            android:theme="@style/FullscreenTheme"/>
        <activity android:name=".Coppie"
            android:label="Coppie"
            android:theme="@style/FullscreenTheme"/>
        <activity android:name=".Musica"
            android:label="Musica"
            android:theme="@style/FullscreenTheme"/>
        <activity android:name=".Scuola"
            android:label="Scuola"
            android:theme="@style/FullscreenTheme"/>
        <activity android:name=".Info"
            android:label="Info"
            android:theme="@style/FullscreenTheme"/>
    </application>

</manifest>